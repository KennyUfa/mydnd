meta:
  environment: docker
  toolchain:
    name: docker
build:
  dockerfile: compose.yaml
  skip: false
run:
  image: null
  command: null
  args: null
  persistenceMount: /data
  containerPort: "80"
  servicePort: "80"
